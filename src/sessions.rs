//mod configure;
use rhai::Engine;
use std::path::PathBuf;
/*
struct Session {
    config_path: PathBuf,
    root: PathBuf,
    name: String,
}

impl Session {
    fn new(config_path: PathBuf, root: PathBuf, name: String) -> Self {
        Self {
            config_path,
            root,
            name,
        }
    }
}

struct Interpreter {
    engine: Engine,
}

impl Interpreter {
    fn new() -> Self {
        Self{ engine: Engine::new() }
    }

    fn register_funcs(&mut self) {
        self.engine.register_fn(name, func)
    }
}
*/
